<!DOCTYPE html>
<html>
<head>
	<title><%= @genre.name %></title>
</head>
<body>
	<h1><%= @genre.name %></h1>
	<h2>Songs: <% @genre.songs.each do |song| %>
			<p><%= song.name %></p>
			<p>Link: <a href="/songs/<%= song.slug %>"></a></p>
			<p>Artist: <%= song.artist.name %></p>
			<p>Link: <a href="/artists/<%= song.artist.slug %>"></a></p>
		<% end %>
	</h2>
</body>
</html>